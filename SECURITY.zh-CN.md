# 安全政策

[English](./SECURITY.md)

## 报告安全漏洞

请**不要**通过公开 GitHub Issue、Discussions、Pull Request、截图或社交媒体报告安全漏洞。

请使用以下任一私密渠道：

- [GitHub 私密漏洞报告](https://github.com/frealcat/TextDuet/security/advisories/new)
- 邮箱：[frealcat@gmail.com](mailto:frealcat@gmail.com)

请提供简要描述、受影响版本或 commit、复现步骤、影响范围，以及理解问题所需的最小 PoC。不得包含真实 API Key、账号凭据、私密 URL、私密网页正文或未脱敏的请求/响应体。我们会在七个自然日内确认收到报告，在调查期间提供状态更新，并在修复可用后协调披露。

## 范围

以下问题尤其欢迎报告：

- API Key、密码、加密、存储或可信扩展上下文边界。
- 未授权网页访问、数据暴露、消息校验或 Provider 请求构造。
- 远程代码执行、不安全的模型输出渲染、依赖供应链风险或 Manifest 权限扩大。
- 本地保险箱锁定时仍可访问持久译文缓存。

项目目前不提供付费漏洞赏金。服务可用性、Provider 输出质量、模型账单争议、网站访问限制和不支持浏览器的行为，一般属于支持或兼容性问题，而不是安全漏洞。

## 支持版本

在 `0.2.0` 准备期间，`main` 分支及其候选版本属于支持范围，欢迎报告问题。正式发布后，当前 `0.2.x` 版本线将成为公开支持线。历史 `0.1.0` 本地安装资料从未公开发布，仅保留为项目历史。

## 安全设计边界

TextDuet 是本地优先项目，不运营翻译中转服务器。用户显式启动翻译后，符合条件的网页文本会直接发送到用户选择的 Provider。API Key 不会返回给网页或内容脚本。会话模式 Key 会在 Chrome 重启后清除；本地模式 Key 与持久缓存使用用户密码解锁的保险箱。上述控制可降低风险，但不能使扩展或第三方 Provider 绝对安全。用户应避免向未审阅其政策的 Provider 发送敏感内容。
