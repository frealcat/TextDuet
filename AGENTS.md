# TextDuet Agent Instructions

任何 Agent 在本仓库内分析、设计、编码、测试、审查或更新文档前，必须先完整阅读根目录 [`AGENT_DEV.md`](./AGENT_DEV.md)，再按其中的任务路由读取 `agent-dev/` 对应分层规则。

- `AGENT_DEV.md` 是唯一控制平面入口；本文件不复制具体规则。
- 规则适用于整个仓库及每一次开发、修复、重构、审查和发布准备。
- 子目录若未来新增 `AGENTS.md`，只能收紧其作用域内规则，不得降低根规则要求。
- 若无法读取必读文件，停止修改并向用户报告，不得凭记忆继续。
