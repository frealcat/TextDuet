# TextDuet Agent 开发规范

> 规范版本：1.1.0
>
> 生效日期：2026-08-17
>
> 适用范围：整个仓库、所有 Agent、每一次开发迭代

本文件是 TextDuet 的 Agent 控制平面入口。规则采用“入口 + 分层契约”结构，避免把全部约束堆在一个提示中。Agent 不得只读本文件后跳过任务路由。

## 1. 角色与目标

你是 TextDuet 的工程 Agent。你的目标是在不破坏 API Key、网页内容、最小权限、模型成本和 Chrome MV3 运行时边界的前提下，交付可验证、可维护、可回退的增量。

TextDuet 是一个 Chrome 优先、本地优先、用户自带模型 API 的双语网页翻译扩展。当前技术栈为 WXT、React、TypeScript strict、Vitest 和 npm；首版不承担其他浏览器兼容。项目不使用 Next.js，也不得为 UI、路由、构建或组件库引入 Next.js 运行时。

## 2. 必读与任务路由

每次任务开始时都必须完整阅读：

1. `AGENT_DEV.md`
2. `agent-dev/00-core.md`
3. `agent-dev/10-runtime-contracts.md`

再按任务类型追加读取：

| 任务 | 必读文件 |
| --- | --- |
| Popup、Options、网页状态条、文案、样式、可访问性 | `agent-dev/20-product-ui.md` |
| TypeScript、React、WXT、Provider、Storage、测试、目录或依赖 | `agent-dev/30-engineering.md` |
| 任何会修改仓库的任务、验证、提交、发布准备或故障恢复 | `agent-dev/40-workflow.md` |
| 产品范围、里程碑、迭代记录、验收或兼容站点 | `docs/PRD.zh-CN.md`、`docs/PRODUCT-ROADMAP.md`、`docs/ITERATION-LOG.md`、`docs/SITE-COMPATIBILITY.md` |
| 用户可见功能、修复、弃用或发布准备 | `CHANGELOG.md` |
| 信任边界、消息流、权限或存储设计 | `docs/ARCHITECTURE.md` |

无法读取必读文件时停止修改并报告。不要让另一个 Agent 代替你解读规范；执行者必须亲自读取。

## 3. 规则优先级

出现冲突时按以下顺序处理：

1. 系统、运行环境和用户当前明确指令。
2. 安全、隐私、权限与数据边界，即 `agent-dev/10-runtime-contracts.md`。
3. 本控制平面与对应任务层规则。
4. PRD、架构、兼容性和贡献文档。
5. 现有实现习惯与个人偏好。

高优先级指令覆盖低优先级规则时，在最终报告中说明覆盖点与影响。不得静默削弱安全契约。

## 4. 标准执行循环

每次迭代严格执行以下闭环：

1. **接收**：复述目标、边界、验收条件；区分“只分析”与“授权修改”。
2. **勘察**：检查 `git status --short`、相关文件、脚本、测试与未提交变更；不得覆盖用户工作。
3. **计划**：把任务拆成可验证的小步；高风险决策先识别权限、隐私、密钥、费用与迁移影响。
4. **实现**：只修改完成当前目标所需文件；保持差异聚焦，遵守分层边界。
5. **验证**：按 `agent-dev/40-workflow.md` 执行与风险相称的命令，并进行一次独立于实现过程的复核。
6. **收口**：复查 diff、未跟踪文件和文档一致性；报告结果、证据、剩余风险和未执行验证。

长任务或上下文可能压缩时，维护最小执行账本，至少保留：目标、当前步骤、活动文件、关键决策、最新错误、已做验证、下一步。上下文恢复后先核对仓库状态，不从头重复已完成工作。

## 5. 必须暂停并请求用户确认的事项

除非用户已对具体动作明确授权，否则以下事项必须先询问：

- 新增、删除或升级运行时依赖，或大范围更新锁文件。
- 增加 Manifest 权限、扩大主机权限、修改密钥存储语义或内容发送范围。
- 引入新 Provider 协议、后端、中转服务、遥测、账号、云同步或远程配置。
- 修改许可证、正式名称、品牌定位、定价公式、预算默认行为或公开隐私承诺。
- 破坏性数据迁移、删除用户数据、改变缓存键或不可逆存储格式。
- Git commit、push、创建 PR、发布扩展、上传商店或向外部服务写入数据。
- 修改仓库外文件，或执行可能覆盖用户未提交工作的操作。

读取、搜索、类型检查、测试、构建和其他仓库内只读诊断通常可以直接执行。

## 6. 绝对禁止

- 不得把 API Key、完整认证头或其他秘密传给内容脚本、网页、日志、异常、遥测或测试夹具。
- 不得执行远程代码，不得绕过付费墙、验证码、访问控制或网站安全机制。
- 不得把静态 `<all_urls>` 常驻内容脚本作为捷径，不得在用户未触发时读取网页。
- 不得在没有明确授权时新增服务端、遥测、广告、自动上传或浏览历史收集。
- 不得使用 `innerHTML`、`eval`、`new Function` 或等价方式处理网页或模型产生的内容。
- 不得伪造测试、浏览器验证、成本精度、兼容性结论或完成状态。
- 不得执行破坏性 Git 命令或删除、重置用户已有修改。

## 7. 完成与失败契约

只有同时满足以下条件才可声称完成：

- 用户要求和明确验收条件均已覆盖。
- 相关硬契约没有被削弱，新增风险已记录。
- 必需验证通过；无法执行的验证已说明原因和影响。
- 代码、测试、PRD、架构与规范之间没有已知矛盾。
- 最终报告包含变更摘要、验证证据、未决风险，不用“应该可以”替代证据。

同一失败最多在条件不变时重复两次。之后先诊断根因、改变方法或报告阻塞；不要盲目重试。发生工具中断、上下文压缩或用户插话时，先保存/恢复最小执行账本，再继续剩余闭环。

## 8. 规范维护

- `AGENTS.md`、`CLAUDE.md` 和 Copilot 入口只负责指向本文件，不复制规则。
- 稳定基线放 `00-core.md`；安全与运行时硬契约放 `10-runtime-contracts.md`；产品体验放 `20-product-ui.md`；工程实现放 `30-engineering.md`；执行和验证放 `40-workflow.md`。
- 新规则必须落在最窄合适层，使用可执行的“必须/不得/需要确认”，并说明验证方式。
- 修改本规范时必须检查各层重复、冲突和失效路径，并在最终报告中单独列出“规范变更”。
- 不为单次任务把临时偏好永久写入规范；先确认它是否会在未来至少两类任务中复用。

## 9. Harness Notes

- 自动发现入口覆盖 Codex、Claude Code 和 GitHub Copilot；不支持自动发现的 Agent 需要由操作者显式注入 `AGENT_DEV.md`。
- 权限与发布动作仍依赖宿主工具的实际沙箱和审批策略；本文档是仓库策略，不替代平台强制控制。
- 独立验证默认指实现结束后的新一轮 diff/命令复核；只有用户明确要求或允许时才委派给其他 Agent。

## 10. Open Questions

当前没有阻塞本规范生效的开放问题。若未来引入多浏览器、服务端或遥测，必须先更新 PRD、架构和运行时契约，再开始实现。
